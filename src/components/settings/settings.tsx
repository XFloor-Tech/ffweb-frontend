import type { FC } from "react";
import { cn } from "@/lib/utils";

type Props = {
  className?: string;
};

const Settings: FC<Props> = ({ className }) => {
  return (
    <div></div>
  );
};

export { Settings };
